query LatestRates($base: String = "BRL", $quotes: [String!] = ["USD", "EUR"]) {
    latest(baseCurrency: $base, quoteCurrencies: $quotes) {
        baseCurrency
        quoteCurrency
        quote
        date
    }
}