query LatestRates($base: String = "BRL", $quotes: [String!] = ["USD", "EUR", "BTC"]) {
    latest(baseCurrency: $base, quoteCurrencies: $quotes) {
        baseCurrency
        quoteCurrency
        quote
        date
    }
}